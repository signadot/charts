# This file is generated. Do not edit.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  name: routeserver
  namespace: signadot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: routeserver
  template:
    metadata:
      annotations:
        {{- range $key, $val := .Values.commonAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        cluster-config-checksum: '{{ include (print $.Template.BasePath "/cluster-config-configmap.yaml") . | sha256sum }}'
      labels:
        {{- range $key, $val := .Values.commonLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        app: routeserver
    spec:
      affinity:
        nodeAffinity:
          {{- $na := include "valuesDefault" (list .Values "" "routeServer" "nodeAffinity") }}{{- if $na }}
          {{- $na | nindent 10 }}
          {{- else }} {}{{- end }}
      containers:
      - image: |-
          {{ include "valuesDefault" (list .Values "signadot/route-server:v1.3.1" "routeServer" "image") }}
        imagePullPolicy: |-
          {{ include "valuesDefault" (list .Values "IfNotPresent" "routeServer" "imagePullPolicy") }}
        name: routeserver
        ports:
        - containerPort: 7777
          name: grpc
        - containerPort: 7778
          name: http-api
        - containerPort: 8080
          name: http-legacy
        - containerPort: 9090
          name: http-metrics
        resources:
          {{- $r := include "valuesDefault" (list .Values "" "routeServer" "resources") }}{{- if $r }}
          {{- $r | nindent 10 }}
          {{- else }}
          limits:
            memory: "2Gi"
          requests:
            memory: "128Mi"
          {{- end }}
        volumeMounts:
        - name: signadot-config
          mountPath: /etc/signadot/
          readOnly: true
      imagePullSecrets:
        {{- if .Values.imagePullSecrets }}
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
        {{- else }} []{{- end }}
      serviceAccountName: routeserver
      tolerations:
        {{- $t := include "valuesDefault" (list .Values "" "routeServer" "tolerations") }}{{- if $t }}
        {{- $t | nindent 8 }}
        {{- else }} []{{- end }}
      volumes:
      - name: signadot-config
        projected:
          sources:
          - configMap:
              name: cluster-config
              items:
              - key: cluster_config.yaml
                path: cluster_config.yaml
