# This file is generated. Do not edit.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  name: signadotroutegroups.signadot.com
spec:
  group: signadot.com
  names:
    kind: SignadotRouteGroup
    listKind: SignadotRouteGroupList
    plural: signadotroutegroups
    shortNames:
    - sdrg
    singular: signadotroutegroup
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: "SignadotRouteGroup is the Schema for the signadotroutegroups API"
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: "RouteGroupSpec defines the desired state of a RouteGroup."
            properties:
              endpoints:
                description: |-
                  Endpoints are the preview endpoints declared for this route group, as
                  named (not resolved) targets. They are carried onto the derived
                  RoutingConfig, whose controller resolves them.
                items:
                  description: |-
                    PreviewEndpoint is a preview endpoint declared by the Signadot control plane
                    on a SignadotSandbox or RouteGroup spec.
                    
                    It *names* a target; it does not resolve one. The control plane has no
                    cluster state, so it cannot know which Service fronts a given workload or
                    which Service port maps to a container port. The sandbox and routegroup
                    controllers carry it through to the derived RoutingConfig unchanged, and the
                    RoutingConfig controller reports what it made of it as a
                    PreviewEndpointStatus on that object's status.
                  properties:
                    id:
                      description: |-
                        ID is an opaque, stable, unguessable identifier for this endpoint,
                        minted by the Signadot control plane.
                        
                        It is the leading label of the preview hostname
                        (https://<id>.<base-domain>) and therefore must be a valid DNS label.
                      maxLength: 63
                      minLength: 1
                      pattern: "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$"
                      type: string
                    name:
                      description: |-
                        Name is the endpoint name as the user declared it, unique within the
                        parent sandbox or route group. It is not used for routing — it exists
                        so logs, metrics and errors can name an endpoint the way a user does.
                      minLength: 1
                      type: string
                    protocol:
                      description: |-
                        Protocol is the protocol used to reach the target.
                        
                        The set here must match what the API accepts when an endpoint is
                        declared. A value the API takes and this rejects is not a validation
                        error on one endpoint -- the whole parent object fails to apply, so the
                        sandbox or route group never reaches the cluster at all.
                      enum:
                      - http
                      - https
                      - grpc
                      type: string
                    target:
                      description: "Target says what this endpoint points at."
                      properties:
                        static:
                          description: "Static is set when Type is Static."
                          properties:
                            host:
                              description: "Host is the host to connect to (no port)."
                              minLength: 1
                              type: string
                            port:
                              description: "Port is the port to connect to."
                              format: int32
                              maximum: 65535
                              minimum: 1
                              type: integer
                          required:
                          - host
                          - port
                          type: object
                        type:
                          description: "PreviewTargetType discriminates the members of PreviewEndpointTarget."
                          enum:
                          - Static
                          - Workload
                          type: string
                        workload:
                          description: "Workload is set when Type is Workload."
                          properties:
                            baseline:
                              description: "Baseline identifies the workload being previewed."
                              properties:
                                apiVersion:
                                  description: "APIVersion specifies the API Version (e.g. \"apps/v1\") of the object to patch."
                                  type: string
                                kind:
                                  description: "Kind specifies the Kind (e.g. \"Deployment\") of the object to patch."
                                  type: string
                                name:
                                  description: "Name specifies the name of the object to patch."
                                  type: string
                                namespace:
                                  description: "Namespace optionally specifies which namespace will be searched."
                                  type: string
                              required:
                              - apiVersion
                              - kind
                              - name
                              type: object
                            port:
                              description: |-
                                Port is the container port to preview. Zero means "infer", which
                                succeeds only when the resolved Service exposes exactly one port.
                              format: int32
                              maximum: 65535
                              minimum: 0
                              type: integer
                          required:
                          - baseline
                          type: object
                      required:
                      - type
                      type: object
                  required:
                  - id
                  - name
                  - protocol
                  - target
                  type: object
                type: array
              routingKey:
                description: |-
                  RoutingKey is a unique short key that can be provided
                  to context propagation mechanisms.
                type: string
              sandboxNames:
                description: |-
                  SandboxNames gives the list of K8s names of SDS objects
                  which belong to this RouteGroup.
                items:
                  type: string
                type: array
              sdRevision:
                description: |-
                  SDRevision indicates the number of updates to the Signadot
                  routegroup specification as hosted by Signadot.
                format: int64
                type: integer
              trafficManagerMatches:
                description: |-
                  TrafficManagerMatches specifies what TrafficManagerPolicy to use
                  for what sandboxed workloads in the routegroup in place of any
                  TrafficManagerPolicies specified by the sandbox itself, which is
                  the default.
                items:
                  description: |-
                    RequestMiddlewareMatch associates an instantiated middleware
                    against workloads in a Sandbox or RouteGroup.
                  properties:
                    instance:
                      description: |-
                        RequestMiddlewareInstance specifies the name of a request middleware
                        and a set of arguments passed to parameters for the request middleware.
                        A list of RequestMiddlewareInstances are associated with workloads in
                        a sandbox or route group, providing the definition of the programmatic
                        treatment of requests sent to that workload.
                      properties:
                        args:
                          description: "Arguments used to instantiate Parameters in the named middleware."
                          items:
                            description: |-
                              Argument is a generic api struct representing arguments to
                              Parameters which are used to instantiate a set of parameters.
                            properties:
                              name:
                                description: "Name indicates the name of the associated parameter."
                                type: string
                              value:
                                description: |-
                                  Value indicates the value passed to the parameter of
                                  the same name.
                                type: string
                              valueFrom:
                                properties:
                                  forward:
                                    type: string
                                required:
                                - forward
                                type: object
                            required:
                            - name
                            - value
                            type: object
                          type: array
                        middleware:
                          description: "Kubernetes resource name of the middleware to instantiate."
                          type: string
                      required:
                      - middleware
                      type: object
                    workload:
                      properties:
                        kind:
                          description: "Kind of the baseline workload."
                          type: string
                        name:
                          description: "Name of the entity within the Kubernetes cluster."
                          type: string
                        namespace:
                          description: "Namespace within which the entity lives in the Kubernetes cluster."
                          type: string
                      required:
                      - kind
                      - name
                      - namespace
                      type: object
                  required:
                  - instance
                  type: object
                type: array
            required:
            - routingKey
            type: object
          status:
            description: "RouteGroupStatus defines status of a RouteGroup"
            properties:
              conditions:
                description: |-
                  Conditions is a list of conditions that matches the conventions expected
                  by kubectl, allowing our CRDs to work with: kubectl wait --for=condition=...
                items:
                  description: |-
                    StatusCondition is a condition struct that matches the conventions expected
                    by kubectl, allowing our CRDs to work with: kubectl wait --for=condition=...
                    
                    StatusConditions must follow the following discipline in reconciliation:
                    The operator MUST always populate all the condition types in a
                    SignadotSandboxStatus struct, irrespective of whether
                    the condition is known or not (see corev1.ConditionUnknown) so that access via
                    a k8s client can determine whether the capability is supported in the operator.
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              endpoints:
                description: |-
                  Endpoints reports what became of Spec.Endpoints, as resolved on the
                  derived RoutingConfig's status: the address for those that could be
                  placed in the cluster, the reason for those that couldn't.
                  
                  The control plane reads sandbox status for its own preview endpoints,
                  not RoutingConfig status directly, so this is the same seam for route
                  groups: without it, a route-group preview endpoint that isn't working
                  has no explanation outside operator logs.
                items:
                  description: |-
                    PreviewEndpointStatus is what the RoutingConfig controller made of a
                    PreviewEndpoint: where it landed in the cluster, or why it couldn't.
                    
                    Every declared endpoint gets one, resolved or not. An endpoint that can't be
                    placed is a state to report, not an absence: "the target isn't there yet" and
                    "we haven't looked" are very different things to whoever is waiting on a
                    preview URL, and only this tells them apart.
                    
                    It rides the RoutingConfig status, and the RoutingConfig's name is the routing
                    key, so the preview server gets the routing context and the target from a
                    single object it already has reason to watch.
                  properties:
                    host:
                      description: |-
                        Host is the in-cluster address to forward to — for a workload target,
                        the DNS name of the baseline Service (e.g. "frontend.default.svc").
                        
                        Empty unless Resolution.Resolved.
                      type: string
                    id:
                      description: |-
                        ID, Name and Protocol are carried through from the PreviewEndpoint
                        this describes.
                      type: string
                    name:
                      type: string
                    port:
                      description: |-
                        Port is the port on Host — for a workload target, the Service port,
                        not the container port.
                        
                        Zero unless Resolution.Resolved.
                      format: int32
                      type: integer
                    protocol:
                      type: string
                    resolution:
                      description: |-
                        Resolution says whether Host and Port below are usable.
                        
                        Optional in the schema, though this controller always sets it: a
                        required field here would mean a controller older than it can't write
                        status at all, rejected by the apiserver rather than degrading. Absent
                        only in status written by such a controller.
                      properties:
                        reason:
                          description: |-
                            Reason says why the endpoint couldn't be placed, and is empty when it
                            could. It is meant to be read by a person: a target that hasn't been
                            created yet, a port that matches several Services, and a target naming a
                            kind we don't fork all land here.
                          type: string
                        resolved:
                          description: |-
                            Resolved is true when the endpoint was placed and Host and Port on the
                            enclosing PreviewEndpointStatus are usable.
                          type: boolean
                      required:
                      - resolved
                      type: object
                  required:
                  - id
                  - name
                  - protocol
                  type: object
                type: array
              observedGeneration:
                description: |-
                  ObservedGeneration provides the Generation of the last
                  RouteGroup seen by the controller for RouteGroups.
                format: int64
                type: integer
              readySandboxes:
                description: |-
                  ReadySandboxes is the number of sandboxes which were ready
                  during the last reconcile.
                format: int32
                type: integer
              totalSandboxes:
                description: |-
                  TotalSandboxes is the total number of sandboxes in the
                  corresponding Spec.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
