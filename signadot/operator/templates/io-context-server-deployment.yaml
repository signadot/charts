# This file is generated. Do not edit.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  name: io-context-server
  namespace: signadot
spec:
  replicas:
    {{- include "valuesDefault" (list .Values 1 "ioContextServer" "replicas") | trim | printf " %s" }}
  selector:
    matchLabels:
      app: io-context-server
  template:
    metadata:
      annotations:
        {{- range $key, $val := .Values.commonAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
      labels:
        {{- range $key, $val := .Values.commonLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        app: io-context-server
    spec:
      affinity:
        nodeAffinity:
          {{- $na := include "valuesDefault" (list .Values "" "ioContextServer" "nodeAffinity") }}{{- if $na }}
          {{- $na | nindent 10 }}
          {{- else }} {}{{- end }}
      containers:
      - command:
        - /app/io-context-server
        - "-tls=secretns=signadot"
        - "-port=8443"
        env:
        - name: LOG_LEVEL
          value: |-
            {{ include "valuesDefaultStr" (list .Values "ioContextServer" "logLevel") }}
        image: |-
          {{ include "operatorImage" (list .Values "" "signadot" "v1.3.2" "io-context-server" "ioContextServer") }}
        imagePullPolicy: |-
          {{ include "valuesDefault" (list .Values "IfNotPresent" "ioContextServer" "imagePullPolicy") }}
        name: io-context-server
        ports:
        - containerPort: 8443
        resources:
          {{- $r := include "valuesDefault" (list .Values "" "ioContextServer" "resources") }}{{- if $r }}
          {{- $r | nindent 10 }}
          {{- else }}
          limits:
            memory: "1Gi"
          requests:
            memory: "128Mi"
          {{- end }}
      imagePullSecrets:
        {{- if .Values.imagePullSecrets }}
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
        {{- else }} []{{- end }}
      serviceAccountName: io-context-server
      tolerations:
        {{- $t := include "valuesDefault" (list .Values "" "ioContextServer" "tolerations") }}{{- if $t }}
        {{- $t | nindent 8 }}
        {{- else }} []{{- end }}
