# This file is generated. Do not edit.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  name: tunnel-api
  namespace: signadot
spec:
  replicas:
    {{- include "valuesDefault" (list .Values 1 "tunnel" "api" "replicas") | trim | printf " %s" }}
  selector:
    matchLabels:
      app: tunnel-api
  strategy:
    {{- with .Values.tunnel }}{{- with .api }}{{- with .strategy }}
    {{ toYaml . | nindent 4 }}
    {{- else }} {}{{- end }}{{- end }}{{- end }}
  template:
    metadata:
      annotations:
        {{- range $key, $val := .Values.commonAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
      labels:
        {{- range $key, $val := .Values.commonLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        app: tunnel-api
    spec:
      affinity:
        nodeAffinity:
          {{- $na := include "valuesDefault" (list .Values "" "tunnel" "api" "nodeAffinity") }}{{- if $na }}
          {{- $na | nindent 10 }}
          {{- else }} {}{{- end }}
      containers:
      - env:
        - name: LOG_LEVEL
          value: |-
            {{ include "valuesDefaultStr" (list .Values "tunnel" "api" "logLevel") }}
        image: |-
          {{ include "operatorImage" (list .Values "" "signadot" "v1.4.0" "tunnel-api" "tunnel" "api") }}
        imagePullPolicy: |-
          {{ include "valuesDefault" (list .Values "IfNotPresent" "tunnel" "api" "imagePullPolicy") }}
        name: tunnel-api
        ports:
        - containerPort: 9070
        resources:
          {{- $r := include "valuesDefault" (list .Values "" "tunnel" "api" "resources") }}{{- if $r }}
          {{- $r | nindent 10 }}
          {{- else }}
          limits:
            memory: 1Gi
          requests:
            memory: 128Mi
          {{- end }}
      imagePullSecrets:
        {{- if .Values.imagePullSecrets }}
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
        {{- else }} []{{- end }}
      serviceAccountName: tunnel-api
      tolerations:
        {{- $t := include "valuesDefault" (list .Values "" "tunnel" "api" "tolerations") }}{{- if $t }}
        {{- $t | nindent 8 }}
        {{- else }} []{{- end }}
