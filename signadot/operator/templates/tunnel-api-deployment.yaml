# This file is generated. Do not edit.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  name: tunnel-api
  namespace: signadot
spec:
  replicas:
    {{- with .Values.tunnel }}{{- with .api }}{{- with .replicas }} {{ . }}
    {{- else }} 1{{- end }}{{- else }} 1{{- end }}{{- else }} 1{{- end }}
  selector:
    matchLabels:
      app: tunnel-api
  strategy:
    {{- with .Values.tunnel }}{{- with .api }}{{- with .strategy }}
    {{ toYaml . | nindent 4 }}
    {{- else }} {}{{- end }}{{- end }}{{- end }}
  template:
    metadata:
      annotations:
        {{- range $key, $val := .Values.commonAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
      labels:
        {{- range $key, $val := .Values.commonLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        app: tunnel-api
    spec:
      containers:
      - env:
        - name: EXTERNAL_DNS_SERVER
          value: |
            {{ with .Values }}{{ with .tunnel }}{{ with .config }}{{ with .externalDNS -}}
            {{ with .server }}{{ . | quote }}{{- else -}}""{{- end }}
            {{- else -}}""{{- end }}{{- else -}}""{{- end }}
            {{- else -}}""{{- end }}{{- else -}}""{{- end }}
        - name: EXTERNAL_DNS_SYNC_INTERVAL
          value: |
            {{ with .Values }}{{ with .tunnel }}{{ with .config }}{{ with .externalDNS -}}
            {{ with .syncInterval }}{{ . | quote }}{{- else -}}""{{- end }}
            {{- else -}}""{{- end }}{{- else -}}""{{- end }}
            {{- else -}}""{{- end }}{{- else -}}""{{- end }}
        image: |-
          {{ include "valuesDefault" (list .Values "signadot/tunnel-api:v1.3.0" "tunnel" "api" "image") }}
        imagePullPolicy: |-
          {{ include "valuesDefault" (list .Values "IfNotPresent" "tunnel" "api" "imagePullPolicy") }}
        name: tunnel-api
        ports:
        - containerPort: 9070
        resources:
          {{- $r := include "valuesDefault" (list .Values "" "tunnel" "api" "resources") }}{{- if $r }}
          {{- $r | nindent 10 }}
          {{- else }}
          limits:
            memory: "1Gi"
          requests:
            memory: "128Mi"
          {{- end }}
      imagePullSecrets:
        {{- if .Values.imagePullSecrets }}
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
        {{- else }} []{{- end }}
      serviceAccountName: tunnel-api
