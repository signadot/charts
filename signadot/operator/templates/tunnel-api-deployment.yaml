# This file is generated. Do not edit.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  name: tunnel-api
  namespace: signadot
spec:
{{- if and .Values .Values.tunnel .Values.tunnel.api .Values.tunnel.api.replicas }}
  replicas: {{ .Values.tunnel.api.replicas }}
{{- end }}
  selector:
    matchLabels:
      app: tunnel-api
{{- if and .Values .Values.tunnel .Values.tunnel.api .Values.tunnel.api.strategy }}
  strategy:
{{ toYaml .Values.tunnel.api.strategy | indent 10 }}
{{- end }}
  template:
    metadata:
      annotations:
        {{- range $key, $val := .Values.commonAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
      labels:
        app: tunnel-api
        {{- range $key, $val := .Values.commonLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
    spec:
      containers:
      - args:
        - --external-dns-server={{ with .Values }}{{ with .tunnel }}{{ with .config }}{{ with .externalDNS }}{{ .server }}{{- else -}}""{{- end }}{{- else -}}""{{- end }}{{- else -}}""{{- end }}{{- else -}}""{{- end }}
        - --external-dns-resync-interval={{ with .Values }}{{ with .tunnel }}{{ with .config }}{{ with .externalDNS }}{{ .syncInterval }}{{- else -}}"30"{{- end }}{{- else -}}"30"{{- end }}{{- else -}}"30"{{- end }}{{- else -}}"30"{{- end }}
        image: {{ with .Values }}{{ with .tunnel }}{{ with .api }}{{ .image  | quote}}{{- else -}}signadot/tunnel-api:v0.13.0-xrc.1{{- end }}{{- else -}}signadot/tunnel-api:v0.13.0-xrc.1{{- end }}{{- else -}}signadot/tunnel-api:v0.13.0-xrc.1{{- end }}
        imagePullPolicy: {{ with .Values }}{{ with .tunnel }}{{ with .api }}{{ .imagePullPolicy  | quote}}{{- else -}}IfNotPresent{{- end }}{{- else -}}IfNotPresent{{- end }}{{- else -}}IfNotPresent{{- end }}
        name: tunnel-api
        ports:
        - containerPort: 9070
{{- if and .Values .Values.tunnel .Values.tunnel.api .Values.tunnel.api.resources }}
        resources:
{{ toYaml .Values.tunnel.api.resources | indent 10 }}
{{- end }}
      serviceAccountName: tunnel-api
