# This file is generated. Do not edit.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  name: tunnel-proxy
  namespace: signadot
spec:
  replicas:
    {{- include "valuesDefault" (list .Values 1 "tunnel" "proxy" "replicas") | trim | printf " %s" }}
  selector:
    matchLabels:
      app: tunnel-proxy
  strategy:
    {{- with .Values.tunnel }}{{- with .proxy }}{{- with .strategy }}
    {{ toYaml . | nindent 4 }}
    {{- else }} {}{{- end }}{{- end }}{{- end }}
  template:
    metadata:
      annotations:
        {{- range $key, $val := .Values.commonAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- if and (hasKey .Values "linkerd") (hasKey .Values.linkerd "enabled") .Values.linkerd.enabled }}
        {{- if and (hasKey .Values.linkerd "operator") (hasKey .Values.linkerd.operator "podAnnotations") .Values.linkerd.operator.podAnnotations }}
        {{- range $key, $val := .Values.linkerd.operator.podAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- else }}
        linkerd.io/inject: "enabled"
        {{- end }}
        {{- end }}
      labels:
        {{- range $key, $val := .Values.commonLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- if and (hasKey .Values "istio") (hasKey .Values.istio "enabled") .Values.istio.enabled }}
        {{- if and (hasKey .Values.istio "operator") (hasKey .Values.istio.operator "podLabels") .Values.istio.operator.podLabels }}
        {{- range $key, $val := .Values.istio.operator.podLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- else }}
        sidecar.istio.io/inject: "true"
        {{- end }}
        {{- end }}
        app: tunnel-proxy
    spec:
      affinity:
        nodeAffinity:
          {{- $na := include "valuesDefault" (list .Values "" "tunnel" "proxy" "nodeAffinity") }}{{- if $na }}
          {{- $na | nindent 10 }}
          {{- else }} {}{{- end }}
      containers:
      - env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: LOG_LEVEL
          value: |-
            {{ include "valuesDefaultStr" (list .Values "tunnel" "proxy" "logLevel") }}
        - name: DISABLE_SSH_TUNNEL
          value: |-
            {{ include "valuesDefaultStr" (list .Values "tunnel" "config" "disableSSH") }}
        - name: DISABLE_XAP_TUNNEL
          value: |-
            {{ include "valuesDefaultStr" (list .Values "tunnel" "config" "disableXAP") }}
        image: |-
          {{ include "operatorImage" (list .Values "" "signadot" "v1.4.0" "tunnel-proxy" "tunnel" "proxy") }}
        imagePullPolicy: |-
          {{ include "valuesDefault" (list .Values "IfNotPresent" "tunnel" "proxy" "imagePullPolicy") }}
        name: tunnel-proxy
        ports:
        - containerPort: 1080
        - containerPort: 2222
        - containerPort: 7777
        - containerPort: 8001
        resources:
          {{- $r := include "valuesDefault" (list .Values "" "tunnel" "proxy" "resources") }}{{- if $r }}
          {{- $r | nindent 10 }}
          {{- else }}
          limits:
            memory: 2Gi
          requests:
            memory: 128Mi
          {{- end }}
      imagePullSecrets:
        {{- if .Values.imagePullSecrets }}
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
        {{- else }} []{{- end }}
      serviceAccountName: tunnel-proxy
      tolerations:
        {{- $t := include "valuesDefault" (list .Values "" "tunnel" "proxy" "tolerations") }}{{- if $t }}
        {{- $t | nindent 8 }}
        {{- else }} []{{- end }}
