# This file is generated. Do not edit.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  name: routingconfigs.signadot.com
spec:
  group: signadot.com
  names:
    kind: RoutingConfig
    listKind: RoutingConfigList
    plural: routingconfigs
    shortNames:
    - sdrc
    singular: routingconfig
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: "RoutingConfigSpec defines the desired state of a sandbox router"
            properties:
              endpoints:
                description: |-
                  Endpoints are the preview endpoints for this routing key, carried
                  through unresolved from the sandbox or route group that declares them.
                  The RoutingConfig controller resolves them against cluster state and
                  publishes the result on Status.Endpoints.
                  
                  Unlike Workloads, these identify *baselines*: the preview server
                  forwards to the resolved Service and injects this RoutingConfig's key,
                  leaving fork selection to devmesh on the downstream hops.
                items:
                  description: |-
                    PreviewEndpoint is a preview endpoint declared by the Signadot control plane
                    on a SignadotSandbox or RouteGroup spec.
                    
                    It *names* a target; it does not resolve one. The control plane has no
                    cluster state, so it cannot know which Service fronts a given workload or
                    which Service port maps to a container port. The sandbox and routegroup
                    controllers carry it through to the derived RoutingConfig unchanged, and the
                    RoutingConfig controller reports what it made of it as a
                    PreviewEndpointStatus on that object's status.
                  properties:
                    id:
                      description: |-
                        ID is an opaque, stable, unguessable identifier for this endpoint,
                        minted by the Signadot control plane.
                        
                        It is the leading label of the preview hostname
                        (https://<id>.<base-domain>) and therefore must be a valid DNS label.
                      maxLength: 63
                      minLength: 1
                      pattern: "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$"
                      type: string
                    name:
                      description: |-
                        Name is the endpoint name as the user declared it, unique within the
                        parent sandbox or route group. It is not used for routing — it exists
                        so logs, metrics and errors can name an endpoint the way a user does.
                      minLength: 1
                      type: string
                    protocol:
                      description: |-
                        Protocol is the protocol used to reach the target.
                        
                        The set here must match what the API accepts when an endpoint is
                        declared. A value the API takes and this rejects is not a validation
                        error on one endpoint -- the whole parent object fails to apply, so the
                        sandbox or route group never reaches the cluster at all.
                      enum:
                      - http
                      - https
                      - grpc
                      type: string
                    target:
                      description: "Target says what this endpoint points at."
                      properties:
                        static:
                          description: "Static is set when Type is Static."
                          properties:
                            host:
                              description: "Host is the host to connect to (no port)."
                              minLength: 1
                              type: string
                            port:
                              description: "Port is the port to connect to."
                              format: int32
                              maximum: 65535
                              minimum: 1
                              type: integer
                          required:
                          - host
                          - port
                          type: object
                        type:
                          description: "PreviewTargetType discriminates the members of PreviewEndpointTarget."
                          enum:
                          - Static
                          - Workload
                          type: string
                        workload:
                          description: "Workload is set when Type is Workload."
                          properties:
                            baseline:
                              description: "Baseline identifies the workload being previewed."
                              properties:
                                apiVersion:
                                  description: "APIVersion specifies the API Version (e.g. \"apps/v1\") of the object to patch."
                                  type: string
                                kind:
                                  description: "Kind specifies the Kind (e.g. \"Deployment\") of the object to patch."
                                  type: string
                                name:
                                  description: "Name specifies the name of the object to patch."
                                  type: string
                                namespace:
                                  description: "Namespace optionally specifies which namespace will be searched."
                                  type: string
                              required:
                              - apiVersion
                              - kind
                              - name
                              type: object
                            port:
                              description: |-
                                Port is the container port to preview. Zero means "infer", which
                                succeeds only when the resolved Service exposes exactly one port.
                              format: int32
                              maximum: 65535
                              minimum: 0
                              type: integer
                          required:
                          - baseline
                          type: object
                      required:
                      - type
                      type: object
                  required:
                  - id
                  - name
                  - protocol
                  - target
                  type: object
                type: array
              sandboxedWorkloads:
                description: |-
                  List of workloads for which the RoutingConfig controller
                  will manage routing.  These workloads should identify forks,
                  not baselines.
                items:
                  properties:
                    kind:
                      enum:
                      - ForkedWorkload
                      - SignadotExternalWorkload
                      - VirtualWorkload
                      type: string
                    middlewares:
                      description: |-
                        RequestMiddlewares specifies what RequestMiddlewares to use
                        for what sandboxed workloads associated with the routing key.  The request
                        middlewares used to process messages destined for .Workloads[i] is .RequestMiddlewares[i].
                      items:
                        description: |-
                          RequestMiddlewareInstance specifies the name of a request middleware
                          and a set of arguments passed to parameters for the request middleware.
                          A list of RequestMiddlewareInstances are associated with workloads in
                          a sandbox or route group, providing the definition of the programmatic
                          treatment of requests sent to that workload.
                        properties:
                          args:
                            description: "Arguments used to instantiate Parameters in the named middleware."
                            items:
                              description: |-
                                Argument is a generic api struct representing arguments to
                                Parameters which are used to instantiate a set of parameters.
                              properties:
                                name:
                                  description: "Name indicates the name of the associated parameter."
                                  type: string
                                value:
                                  description: |-
                                    Value indicates the value passed to the parameter of
                                    the same name.
                                  type: string
                                valueFrom:
                                  properties:
                                    forward:
                                      type: string
                                  required:
                                  - forward
                                  type: object
                              required:
                              - name
                              - value
                              type: object
                            type: array
                          middleware:
                            description: "Kubernetes resource name of the middleware to instantiate."
                            type: string
                        required:
                        - middleware
                        type: object
                      type: array
                    name:
                      type: string
                    namespace:
                      type: string
                    sandboxName:
                      type: string
                  required:
                  - kind
                  - name
                  - namespace
                  type: object
                type: array
            required:
            - sandboxedWorkloads
            type: object
          status:
            description: "RoutingConfigStatus defines the status of a sandbox router"
            properties:
              conditions:
                description: |-
                  Conditions is a list of conditions that matches the conventions expected
                  by kubectl, allowing our CRDs to work with: kubectl wait --for=condition=...
                items:
                  description: |-
                    StatusCondition is a condition struct that matches the conventions expected
                    by kubectl, allowing our CRDs to work with: kubectl wait --for=condition=...
                    
                    StatusConditions must follow the following discipline in reconciliation:
                    The operator MUST always populate all the condition types in a
                    SignadotSandboxStatus struct, irrespective of whether
                    the condition is known or not (see corev1.ConditionUnknown) so that access via
                    a k8s client can determine whether the capability is supported in the operator.
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              endpoints:
                description: |-
                  Endpoints reports what the controller made of each of Spec.Endpoints:
                  the concrete address for those it could place, and the reason for those
                  it couldn't. There is one entry per declared endpoint, in both cases.
                  This is what the preview server reads.
                  
                  An unresolved endpoint is a normal, transient state while a sandbox is
                  coming up — it resolves once its target exists.
                items:
                  description: |-
                    PreviewEndpointStatus is what the RoutingConfig controller made of a
                    PreviewEndpoint: where it landed in the cluster, or why it couldn't.
                    
                    Every declared endpoint gets one, resolved or not. An endpoint that can't be
                    placed is a state to report, not an absence: "the target isn't there yet" and
                    "we haven't looked" are very different things to whoever is waiting on a
                    preview URL, and only this tells them apart.
                    
                    It rides the RoutingConfig status, and the RoutingConfig's name is the routing
                    key, so the preview server gets the routing context and the target from a
                    single object it already has reason to watch.
                  properties:
                    host:
                      description: |-
                        Host is the in-cluster address to forward to — for a workload target,
                        the DNS name of the baseline Service (e.g. "frontend.default.svc").
                        
                        Empty unless Resolution.Resolved.
                      type: string
                    id:
                      description: |-
                        ID, Name and Protocol are carried through from the PreviewEndpoint
                        this describes.
                      type: string
                    name:
                      type: string
                    port:
                      description: |-
                        Port is the port on Host — for a workload target, the Service port,
                        not the container port.
                        
                        Zero unless Resolution.Resolved.
                      format: int32
                      type: integer
                    protocol:
                      type: string
                    resolution:
                      description: |-
                        Resolution says whether Host and Port below are usable.
                        
                        Optional in the schema, though this controller always sets it: a
                        required field here would mean a controller older than it can't write
                        status at all, rejected by the apiserver rather than degrading. Absent
                        only in status written by such a controller.
                      properties:
                        reason:
                          description: |-
                            Reason says why the endpoint couldn't be placed, and is empty when it
                            could. It is meant to be read by a person: a target that hasn't been
                            created yet, a port that matches several Services, and a target naming a
                            kind we don't fork all land here.
                          type: string
                        resolved:
                          description: |-
                            Resolved is true when the endpoint was placed and Host and Port on the
                            enclosing PreviewEndpointStatus are usable.
                          type: boolean
                      required:
                      - resolved
                      type: object
                  required:
                  - id
                  - name
                  - protocol
                  type: object
                type: array
              observedGeneration:
                description: |-
                  ObservedGeneration provides the Generation of the last IstioRoute seen by
                  the controller.
                format: int64
                type: integer
              processedWorkloads:
                description: |-
                  ProcessedWorkloads is the list of sandboxed workloads as seen by the
                  controller, along with its routing information
                items:
                  properties:
                    baseline:
                      description: |-
                        BaselineUID gives the UID of the baseline workload from which the sandboxed
                        workload is derived
                      properties:
                        kind:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                        uid:
                          type: string
                      required:
                      - kind
                      - name
                      - namespace
                      - uid
                      type: object
                    middlewares:
                      description: |-
                        middlewares indicates what middlewares should receive
                        traffic destined for this sandboxed workload.
                      items:
                        description: |-
                          RequestMiddlewareInstance specifies the name of a request middleware
                          and a set of arguments passed to parameters for the request middleware.
                          A list of RequestMiddlewareInstances are associated with workloads in
                          a sandbox or route group, providing the definition of the programmatic
                          treatment of requests sent to that workload.
                        properties:
                          args:
                            description: "Arguments used to instantiate Parameters in the named middleware."
                            items:
                              description: |-
                                Argument is a generic api struct representing arguments to
                                Parameters which are used to instantiate a set of parameters.
                              properties:
                                name:
                                  description: "Name indicates the name of the associated parameter."
                                  type: string
                                value:
                                  description: |-
                                    Value indicates the value passed to the parameter of
                                    the same name.
                                  type: string
                                valueFrom:
                                  properties:
                                    forward:
                                      type: string
                                  required:
                                  - forward
                                  type: object
                              required:
                              - name
                              - value
                              type: object
                            type: array
                          middleware:
                            description: "Kubernetes resource name of the middleware to instantiate."
                            type: string
                        required:
                        - middleware
                        type: object
                      type: array
                    routes:
                      description: |-
                        Routes is the list of the configured routes per service mesh (including
                        the signadot sidecar)
                      properties:
                        devMesh:
                          description: |-
                            DevMesh provides information about the setup of the DevMesh sidecar for
                            this workload
                          properties:
                            enabled:
                              description: |-
                                Enabled means that the DevMesh sidecar container is present and ready in
                                all the baseline workload pods
                              type: boolean
                          required:
                          - enabled
                          type: object
                        externalRouting:
                          description: |-
                            ExternalRouting lists the baseline services whose routing is owned by an
                            external component (see ExternalRoutingAnnotation). No DevMesh, Istio or
                            Gateway API enforcement is created for them, and they don't require a
                            DevMesh sidecar to be routing-ready.
                          items:
                            type: string
                          type: array
                        gatewayAPI:
                          description: |-
                            GatewayAPI is a list of the GatewayAPIRoute objects created for this
                            sandbox
                          items:
                            properties:
                              name:
                                description: "Name of the GatewayAPIRoute"
                                type: string
                              status:
                                description: "Status of the GatewayAPIRoute"
                                properties:
                                  conditions:
                                    description: |-
                                      Conditions is a list of conditions that matches the conventions expected
                                      by kubectl, allowing our CRDs to work with: kubectl wait --for=condition=...
                                    items:
                                      description: |-
                                        StatusCondition is a condition struct that matches the conventions expected
                                        by kubectl, allowing our CRDs to work with: kubectl wait --for=condition=...
                                        
                                        StatusConditions must follow the following discipline in reconciliation:
                                        The operator MUST always populate all the condition types in a
                                        SignadotSandboxStatus struct, irrespective of whether
                                        the condition is known or not (see corev1.ConditionUnknown) so that access via
                                        a k8s client can determine whether the capability is supported in the operator.
                                      properties:
                                        lastTransitionTime:
                                          format: date-time
                                          type: string
                                        message:
                                          type: string
                                        reason:
                                          type: string
                                        status:
                                          type: string
                                        type:
                                          type: string
                                      required:
                                      - status
                                      - type
                                      type: object
                                    type: array
                                  matchedObjects:
                                    description: |-
                                      MatchedObjects is the list of Gateway API HTTPRoutes and GRPCRoutes that
                                      match this route, along with their current status.
                                    items:
                                      properties:
                                        baseline:
                                          description: "Baseline specifies the baseline version of the matched route"
                                          properties:
                                            kind:
                                              description: "Kind specifies the Kind of the matched route (HTTPRoute or GRPCRoute)."
                                              enum:
                                              - HTTPRoute
                                              - GRPCRoute
                                              type: string
                                            name:
                                              description: "Name specifies the name of the matched route"
                                              type: string
                                            namespace:
                                              description: "Namespace specifies the namespace of the matched route"
                                              type: string
                                          required:
                                          - kind
                                          - name
                                          - namespace
                                          type: object
                                        derived:
                                          description: "Derived specifies the derived version of the matched route"
                                          properties:
                                            kind:
                                              description: "Kind specifies the Kind of the matched route (HTTPRoute or GRPCRoute)."
                                              enum:
                                              - HTTPRoute
                                              - GRPCRoute
                                              type: string
                                            name:
                                              description: "Name specifies the name of the matched route"
                                              type: string
                                            namespace:
                                              description: "Namespace specifies the namespace of the matched route"
                                              type: string
                                          required:
                                          - kind
                                          - name
                                          - namespace
                                          type: object
                                        message:
                                          description: "Status related message"
                                          type: string
                                        status:
                                          description: "Status information about the route configuration"
                                          type: string
                                      required:
                                      - baseline
                                      - status
                                      type: object
                                    type: array
                                  observedGeneration:
                                    description: |-
                                      ObservedGeneration provides the Generation of the last GatewayAPIRoute seen by
                                      the controller.
                                    format: int64
                                    type: integer
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        istio:
                          description: "Istio is a list of the IstioRoute objects created for this sandbox"
                          items:
                            properties:
                              name:
                                description: "Name of the IstioRoute"
                                type: string
                              status:
                                description: "Status of the IstioRoute"
                                properties:
                                  conditions:
                                    description: |-
                                      Conditions is a list of conditions that matches the conventions expected
                                      by kubectl, allowing our CRDs to work with: kubectl wait --for=condition=...
                                    items:
                                      description: |-
                                        StatusCondition is a condition struct that matches the conventions expected
                                        by kubectl, allowing our CRDs to work with: kubectl wait --for=condition=...
                                        
                                        StatusConditions must follow the following discipline in reconciliation:
                                        The operator MUST always populate all the condition types in a
                                        SignadotSandboxStatus struct, irrespective of whether
                                        the condition is known or not (see corev1.ConditionUnknown) so that access via
                                        a k8s client can determine whether the capability is supported in the operator.
                                      properties:
                                        lastTransitionTime:
                                          format: date-time
                                          type: string
                                        message:
                                          type: string
                                        reason:
                                          type: string
                                        status:
                                          type: string
                                        type:
                                          type: string
                                      required:
                                      - status
                                      - type
                                      type: object
                                    type: array
                                  matchedVirtualServices:
                                    description: |-
                                      MatchedVirtualServices is a list of the matching virtual services for
                                      this route, along with its status
                                    items:
                                      properties:
                                        message:
                                          description: "Status related message"
                                          type: string
                                        name:
                                          description: "This is the virtual service name"
                                          type: string
                                        namespace:
                                          description: "This is the virtual service namespace"
                                          type: string
                                        status:
                                          description: "Status information about the virtual service configuration"
                                          type: string
                                      required:
                                      - name
                                      - namespace
                                      - status
                                      type: object
                                    type: array
                                  observedGeneration:
                                    description: |-
                                      ObservedGeneration provides the Generation of the last IstioRoute seen by
                                      the controller.
                                    format: int64
                                    type: integer
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                      type: object
                    workload:
                      description: "Workload is a reference to one of the sandboxed workloads from the Spec"
                      properties:
                        kind:
                          enum:
                          - ForkedWorkload
                          - SignadotExternalWorkload
                          - VirtualWorkload
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                        sandboxName:
                          type: string
                      required:
                      - kind
                      - name
                      - namespace
                      type: object
                  required:
                  - baseline
                  - workload
                  type: object
                type: array
              routeServer:
                description: "RouteServer is the information that will be consumed by the route server"
                properties:
                  routes:
                    description: "Routes is the list of routes to be exported to the route server"
                    items:
                      properties:
                        baseline:
                          description: "Baseline workload informaton"
                          properties:
                            generation:
                              description: |-
                                Generation is the latest generation of the underlying baseline workload
                                as seen by the controller
                              format: int64
                              type: integer
                            workloadID:
                              description: "WorkloadID is the UID of the underlying baseline workload."
                              type: string
                          required:
                          - workloadID
                          type: object
                        from:
                          description: "From the specified location"
                          properties:
                            host:
                              description: "Host of this location"
                              type: string
                            port:
                              description: "Port where this location can be accessed"
                              format: int32
                              type: integer
                            workloadPort:
                              description: |-
                                Port where it listens the workload behind this location (if any). In this
                                case of kubernetes services, this is the service target port.
                              format: int32
                              type: integer
                          required:
                          - host
                          - port
                          type: object
                        sandboxName:
                          description: "Sandbox name owner of this route"
                          type: string
                        to:
                          description: "To the specified location"
                          properties:
                            host:
                              description: "Host of this location"
                              type: string
                            port:
                              description: "Port where this location can be accessed"
                              format: int32
                              type: integer
                            workloadPort:
                              description: |-
                                Port where it listens the workload behind this location (if any). In this
                                case of kubernetes services, this is the service target port.
                              format: int32
                              type: integer
                          required:
                          - host
                          - port
                          type: object
                      required:
                      - from
                      - to
                      type: object
                    type: array
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
