# This file is generated. Do not edit.
apiVersion: v1
data:
  cidr.yaml: |
    {{- include "valuesDefaultStr" (list .Values "tunnel" "config" "cidrs") | nindent 4 }}
kind: ConfigMap
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  name: tunnel-cidrs
  namespace: signadot
