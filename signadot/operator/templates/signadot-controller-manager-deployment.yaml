# This file is generated. Do not edit.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
    control-plane: controller-manager
  name: signadot-controller-manager
  namespace: {{ include "operator.namespace" . }}
spec:
  replicas:
    {{- with .Values.controllerManager }}{{- with .replicas }} {{ . }}
    {{- else }} 2{{- end }}{{- else }} 2{{- end }}
  selector:
    matchLabels:
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        {{- range $key, $val := .Values.commonAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        cluster-config-checksum: '{{ include (print $.Template.BasePath "/cluster-config-configmap.yaml") . | sha256sum }}'
      labels:
        {{- range $key, $val := .Values.commonLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        control-plane: controller-manager
    spec:
      affinity:
        nodeAffinity:
          {{- $na := include "valuesDefault" (list .Values "" "controllerManager" "nodeAffinity") }}{{- if $na }}
          {{- $na | nindent 10 }}
          {{- else }} {}{{- end }}
      containers:
      - args:
        - "--leader-elect"
        command:
        - /manager
        env:
        - name: IO_INIT_IMAGE
          value: |-
            {{ include "valuesDefault" (list .Values "signadot/io-init:v1.3.0" "ioInit" "image") }}
        - name: IO_INIT_IMAGE_PULL_POLICY
          value: |-
            {{ include "valuesDefault" (list .Values "IfNotPresent" "ioInit" "imagePullPolicy") }}
        - name: IO_SIDECAR_IMAGE
          value: |-
            {{ include "valuesDefault" (list .Values "signadot/io-sidecar:v1.3.0" "ioSidecar" "image") }}
        - name: IO_SIDECAR_IMAGE_PULL_POLICY
          value: |-
            {{ include "valuesDefault" (list .Values "IfNotPresent" "ioSidecar" "imagePullPolicy") }}
        - name: JOB_EXECUTOR_INIT_IMAGE
          value: |-
            {{ include "valuesDefault" (list .Values "signadot/job-executor-init:v1.3.0" "jobExecutorInit" "image") }}
        - name: JOB_EXECUTOR_INIT_IMAGE_PULL_POLICY
          value: |-
            {{ include "valuesDefault" (list .Values "IfNotPresent" "jobExecutorInit" "imagePullPolicy") }}
        - name: JOB_EXECUTOR_PROXY_IMAGE
          value: |-
            {{ include "valuesDefault" (list .Values "signadot/job-executor-proxy:v1.3.0" "jobExecutorProxy" "image") }}
        - name: JOB_EXECUTOR_PROXY_IMAGE_PULL_POLICY
          value: |-
            {{ include "valuesDefault" (list .Values "IfNotPresent" "jobExecutorProxy" "imagePullPolicy") }}
        - name: ROUTE_SIDECAR_IMAGE
          value: |-
            {{ include "valuesDefault" (list .Values "signadot/route-sidecar:v1.3.0" "routeSidecar" "image") }}
        - name: ROUTE_SIDECAR_IMAGE_PULL_POLICY
          value: |-
            {{ include "valuesDefault" (list .Values "IfNotPresent" "routeSidecar" "imagePullPolicy") }}
        - name: SIDECAR_INIT_IMAGE
          value: |-
            {{ include "valuesDefault" (list .Values "signadot/route-sidecar-init:v1.3.0" "routeInit" "image") }}
        - name: SIDECAR_INIT_IMAGE_PULL_POLICY
          value: |-
            {{ include "valuesDefault" (list .Values "IfNotPresent" "routeInit" "imagePullPolicy") }}
        - name: IO_INIT_IMAGE_PULL_SECRET
          value: |-
            {{ include "valuesDefaultStr" (list .Values "ioInit" "imagePullSecret") }}
        - name: IO_SIDECAR_IMAGE_PULL_SECRET
          value: |-
            {{ include "valuesDefaultStr" (list .Values "ioSidecar" "imagePullSecret") }}
        - name: JOB_EXECUTOR_INIT_IMAGE_PULL_SECRET
          value: |-
            {{ include "valuesDefaultStr" (list .Values "jobExecutorInit" "imagePullSecret") }}
        - name: JOB_EXECUTOR_PROXY_IMAGE_PULL_SECRET
          value: |-
            {{ include "valuesDefaultStr" (list .Values "jobExecutorProxy" "imagePullSecret") }}
        - name: ROUTE_SIDECAR_IMAGE_PULL_SECRET
          value: |-
            {{ include "valuesDefaultStr" (list .Values "routeSidecar" "imagePullSecret") }}
        - name: SIDECAR_INIT_IMAGE_PULL_SECRET
          value: |-
            {{ include "valuesDefaultStr" (list .Values "routeInit" "imagePullSecret") }}
        - name: DEV_MESH_INIT_CONTAINER_RESOURCES
          value: |-
            {{ include "valuesDefaultJson" (list .Values "routeInit" "resources") }}
        - name: DEV_MESH_SIDECAR_CONTAINER_RESOURCES
          value: |-
            {{ include "valuesDefaultJson" (list .Values "routeSidecar" "resources") }}
        - name: IO_INIT_CONTAINER_RESOURCES
          value: |-
            {{ include "valuesDefaultJson" (list .Values "ioInit" "resources") }}
        - name: IO_SIDECAR_CONTAINER_RESOURCES
          value: |-
            {{ include "valuesDefaultJson" (list .Values "ioSidecar" "resources") }}
        - name: ISTIO_ADDITIONAL_ANNOTATIONS
          value: |-
            {{ with .Values }}{{ with .istio }}{{ with .additionalAnnotations }}{{ mustToJson . }}{{- end }}{{- end }}{{- end }}
        - name: ISTIO_ADDITIONAL_LABELS
          value: |-
            {{ with .Values }}{{ with .istio }}{{ with .additionalLabels }}{{ mustToJson . }}{{- end }}{{- end }}{{- end }}
        - name: JOB_EXECUTOR_INIT_CONTAINER_RESOURCES
          value: |-
            {{ include "valuesDefaultJson" (list .Values "jobExecutorInit" "resources") }}
        - name: JOB_EXECUTOR_PROXY_CONTAINER_RESOURCES
          value: |-
            {{ include "valuesDefaultJson" (list .Values "jobExecutorProxy" "resources") }}
        image: |-
          {{ include "valuesDefault" (list .Values "signadot/controller-manager:v1.3.0" "controllerManager" "image") }}
        imagePullPolicy: |-
          {{ include "valuesDefault" (list .Values "IfNotPresent" "controllerManager" "imagePullPolicy") }}
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          {{- $r := include "valuesDefault" (list .Values "" "controllerManager" "resources") }}{{- if $r }}
          {{- $r | nindent 10 }}
          {{- else }}
          limits:
            cpu: "100m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "512Mi"
          {{- end }}
        securityContext:
          allowPrivilegeEscalation: false
        volumeMounts:
        - name: podinfo
          mountPath: /etc/podinfo
        - name: signadot-config
          mountPath: /etc/signadot/
          readOnly: true
      imagePullSecrets:
        {{- if .Values.imagePullSecrets }}
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
        {{- else }} []{{- end }}
      securityContext:
        runAsNonRoot: true
      serviceAccountName: signadot-controller-manager
      terminationGracePeriodSeconds: 10
      tolerations:
        {{- $t := include "valuesDefault" (list .Values "" "controllerManager" "tolerations") }}{{- if $t }}
        {{- $t | nindent 8 }}
        {{- else }} []{{- end }}
      volumes:
      - name: signadot-config
        projected:
          sources:
          - secret:
              items:
              - key: token
                path: token
              name: |-
                {{ include "tokenSecretName" . }}
          - configMap:
              items:
              - key: cluster_config.yaml
                path: cluster_config.yaml
              name: cluster-config
      - name: podinfo
        downwardAPI:
          items:
          - path: labels
            fieldRef:
              fieldPath: metadata.labels
