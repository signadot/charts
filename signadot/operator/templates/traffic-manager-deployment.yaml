# This file is generated. Do not edit.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    {{- range $key, $val := .Values.commonAnnotations }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  labels:
    {{- range $key, $val := .Values.commonLabels }}
    {{ $key | quote }}: {{ $val | quote }}
    {{- end }}
  name: traffic-manager
  namespace: signadot
spec:
  replicas:
    {{- with .Values.trafficManager }}{{- with .replicas }} {{ . }}
    {{- else }} 2{{- end }}{{- else }} 2{{- end }}
  selector:
    matchLabels:
      app: traffic-manager
  template:
    metadata:
      annotations:
        {{- range $key, $val := .Values.commonAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- if and (hasKey .Values "linkerd") (hasKey .Values.linkerd "enabled") .Values.linkerd.enabled }}
        {{- if and (hasKey .Values.linkerd "operator") (hasKey .Values.linkerd.operator "podAnnotations") .Values.linkerd.operator.podAnnotations }}
        {{- range $key, $val := .Values.linkerd.operator.podAnnotations }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- else }}
        linkerd.io/inject: "enabled"
        {{- end }}
        {{- end }}
        cluster-config-checksum: '{{ include (print $.Template.BasePath "/cluster-config-configmap.yaml") . | sha256sum }}'
      labels:
        {{- range $key, $val := .Values.commonLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- range $key, $val := .Values.podLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- if and (hasKey .Values "istio") (hasKey .Values.istio "enabled") .Values.istio.enabled }}
        {{- if and (hasKey .Values.istio "operator") (hasKey .Values.istio.operator "podLabels") .Values.istio.operator.podLabels }}
        {{- range $key, $val := .Values.istio.operator.podLabels }}
        {{ $key | quote }}: {{ $val | quote }}
        {{- end }}
        {{- else }}
        sidecar.istio.io/inject: "true"
        {{- end }}
        {{- end }}
        app: traffic-manager
    spec:
      containers:
      - image: |-
          {{ include "valuesDefault" (list .Values "signadot/traffic-manager:v1.3.0" "trafficManager" "image") }}
        imagePullPolicy: |-
          {{ include "valuesDefault" (list .Values "IfNotPresent" "trafficManager" "imagePullPolicy") }}
        name: traffic-manager
        ports:
        - containerPort: 8080
          name: proxy
        - containerPort: 16001
          name: healthz
        readinessProbe:
          httpGet:
            path: /healthz
            port: 16001
        resources:
          {{- $r := include "valuesDefault" (list .Values "" "trafficManager" "resources") }}{{- if $r }}
          {{- $r | nindent 10 }}
          {{- else }}
          limits:
            memory: "2Gi"
          requests:
            memory: "128Mi"
          {{- end }}
        volumeMounts:
        - name: signadot-config
          mountPath: /etc/signadot/
          readOnly: true
      imagePullSecrets:
        {{- if .Values.imagePullSecrets }}
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
        {{- else }} []{{- end }}
      serviceAccountName: traffic-manager
      volumes:
      - name: signadot-config
        projected:
          sources:
          - secret:
              items:
              - key: token
                path: token
              name: |-
                {{ include "tokenSecretName" . }}
          - configMap:
              items:
              - key: cluster_config.yaml
                path: cluster_config.yaml
              name: cluster-config
